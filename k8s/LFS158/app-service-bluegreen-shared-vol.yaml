apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: app-blue
    label: blue # Initially points to blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer # Or ClusterIP, NodePort depending on your needs
